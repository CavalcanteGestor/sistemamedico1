# ⚠️ ATENÇÃO: Este arquivo é apenas um exemplo
# NUNCA commit credenciais reais no repositório!
# Configure estas variáveis no servidor via .env.production

# URL do seu projeto Supabase
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave anônima do Supabase (pode ser pública, mas proteja seu projeto)
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anon_aqui

# URL da aplicação (produção: https://seu-dominio.com)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ⚠️ CRÍTICO: Service Role Key - NUNCA exponha isso no client-side!
# Esta chave deve estar APENAS no servidor e nunca no repositório
SUPABASE_SERVICE_ROLE_KEY=sua_chave_service_role_aqui

# Evolution API - WhatsApp Integration
NEXT_PUBLIC_EVOLUTION_API_URL=https://sua-evolution-api.com
EVOLUTION_API_KEY=sua_api_key_aqui
EVOLUTION_INSTANCE_NAME=default

# OpenAI - Para geração de mensagens de follow-up e resumos de telemedicina
OPENAI_API_KEY=sua_chave_openai_aqui
# Modelo OpenAI (opcional, padrão: gpt-4o-mini)
# Opções: gpt-4o-mini (padrão), gpt-4o, gpt-5 (se disponível)
OPENAI_MODEL=gpt-4o-mini

# Supabase Management API - Para atualização automática de templates de email
# Obtenha em: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sua_chave_access_token_aqui
# Project Ref (opcional - será extraído da URL se não fornecido)
NEXT_PUBLIC_SUPABASE_PROJECT_REF=yuypxyvvfnnichiwkepm

# Cron Secret Key - Para autenticação de cron jobs (gerar uma chave forte)
# Use: openssl rand -base64 32
CRON_SECRET_KEY=sua_chave_secreta_forte_aqui

